<section>
    <div class="recipe">
        <h1>Recipe: {{recipe.name}}</h1>
        <h3>Description</h3>
        <p>{{recipe.descritption}}</p>
        <div class="row">
            <div class="col-md-6">
                <h4>Ingredients</h4>
                <p>{{recipe.ingredients}}</p>
            </div>
            <div class="col-md-6">
                <h4>Instructions</h4>
                <p>{{recipe.instructions}}</p>
            </div>
        </div>
        <div>
            <p>Created On: {{recipe.post_date}}</p>
        </div>
    </div>
</section>
